using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class fihscript : MonoBehaviour
{
    public int cost;
    // Start is called before the first frame update
    void Start()
    {
        cost = Random.Range(1500, 2001);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
